# Local Next.js apps (no Traefik). From repo root:
#   pnpm --filter @workix/web dev
#   pnpm --filter @workix/learn dev
#   pnpm --filter @workix/admin dev
#   pnpm --filter @workix/api dev
#   pnpm --filter @workix/worker dev
#
# Redis + API/worker:
#   docker compose -f infra/docker-compose.dev.yml up
